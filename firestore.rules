rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows anyone to read and write to the database.
    // This is insecure and should be updated before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
