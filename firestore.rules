rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Autorise la lecture et l'écriture pour toutes les collections,
    // ce qui est nécessaire pour sauvegarder l'état du tableau.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
